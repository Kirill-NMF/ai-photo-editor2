🚀 ISSUE: Hugging Face 500 Error

The problem:

•
The current model timbrooks/instruct-pix2pix is not supported by the Hugging Face Inference API.

•
This causes a 500: No Inference Provider available error.

The reason:

•
Not all models on Hugging Face are available through the Inference API.

•
timbrooks/instruct-pix2pix is one of them.

✅ GOAL: Fix the Hugging Face integration

The solution:

•
Replace the unsupported model with a supported one.

•
Use a model that is specifically designed for image editing and is available through the Inference API.

The new model:

•
nvidia/ChronoEdit-14B-Diffusers

Why this model?

•
✅ Supported by Inference API (through the fal-ai provider)

•
✅ Designed for image editing

•
✅ Official NVIDIA model

•
✅ Recommended in Hugging Face documentation

📝 STRATEGY: Minimal code change

We will make a small change in server/huggingface.ts to:

1.
Use the new model nvidia/ChronoEdit-14B-Diffusers

2.
Specify the provider fal-ai

This will fix the error and enable image editing through Hugging Face.

💻 REFERENCE CODE PROVIDED: Yes

I will provide the exact code snippet for server/huggingface.ts.

🛠️ IMPLEMENTATION INSTRUCTIONS

TASK 1: Update server/huggingface.ts

Action:

•
Open the file server/huggingface.ts

•
Replace the entire content of the file with the code from CODE SNIPPET 1.

Why?

•
This will switch to the new model and provider.

•
It also includes better error handling.

TASK 2: Verify HUGGINGFACE_API_KEY

Action:

•
Ensure the HUGGINGFACE_API_KEY secret is still configured correctly in Replit.

Why?

•
The new provider fal-ai still uses the same Hugging Face token for authentication.

📄 CODE SNIPPETS

CODE SNIPPET 1: server/huggingface.ts

TypeScript


import { HfInference } from "@huggingface/inference";

const hf = new HfInference(process.env.HUGGINGFACE_API_KEY);

export async function editWithHuggingFace(prompt: string, image: Buffer) {
  try {
    const result = await hf.imageToImage(
      {
        data: image,
        model: "nvidia/ChronoEdit-14B-Diffusers",
        parameters: {
          prompt: prompt,
        },
      },
      {
        provider: "fal-ai",
      }
    );
    return result;
  } catch (error) {
    console.error("Hugging Face API error:", error);
    throw new Error(`Failed to edit image with Hugging Face: ${error.message}`);
  }
}


