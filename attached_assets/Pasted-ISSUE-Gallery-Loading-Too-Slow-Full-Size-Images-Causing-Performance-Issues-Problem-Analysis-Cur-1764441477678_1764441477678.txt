ISSUE: Gallery Loading Too Slow - Full-Size Images Causing Performance Issues

Problem Analysis

Current situation:

•
Gallery loads full-size images (5-10 MB each)

•
With 9 projects visible: 45-90 MB total download

•
Loading time on 3G: ~40 seconds

•
Even with lazy loading: still loading large files

Root cause: The gallery displays small preview cards (400x225px) but downloads full-resolution images (2000x3000px+).

Example:

Plain Text


Gallery card size: 400x225px (16:9 aspect ratio)
Actual image size: 3000x4000px (7 MB)
Wasted bandwidth: 99% of the image data is not visible!


GOAL

Implement automatic WebP thumbnail generation to create small, optimized preview images (400x225px, ~25 KB) that load instantly in the gallery.

Expected improvement:

•
Current: 9 images × 7 MB = 63 MB, ~40 seconds (3G)

•
After: 9 WebP thumbnails × 25 KB = 225 KB, ~1.5 seconds (3G) ✅ 96% faster!

STRATEGY

Architecture Overview

Plain Text


User uploads image
     ↓
Server receives full image
     ↓
1. Save original (full size) → /uploads/{id}/original.jpg
2. Generate WebP thumbnail (400x225) → /uploads/{id}/thumb.webp
     ↓
Database stores both URLs:
  - originalUrl: full size (for editor)
  - thumbnailUrl: WebP (for gallery) ← NEW!
     ↓
Gallery uses thumbnailUrl (WebP, 25 KB)
Editor uses originalUrl (original format, full quality)


Technology Choice

Use Sharp library (Node.js standard for image processing):

•
Fast (C++ bindings, 4-5x faster than ImageMagick)

•
Excellent WebP support with quality optimization

•
Automatic resizing, cropping

•
Memory efficient

Why WebP?

•
30% smaller than JPEG at same quality

•
96%+ browser support (all modern browsers)

•
Better compression algorithm

•
Supports transparency (if needed)

Size comparison:

•
JPEG thumbnail (400x225, 80% quality): ~50 KB

•
WebP thumbnail (400x225, 80% quality): ~25 KB ✅ 50% smaller!

IMPLEMENTATION

Step 1: Install Sharp

Bash


npm install sharp
npm install --save-dev @types/sharp


Why Sharp?

•
✅ 4-5x faster than ImageMagick

•
✅ Low memory usage

•
✅ Best-in-class WebP encoder

•
✅ Built-in quality optimization




Step 2: Update Database Schema

Add thumbnailUrl column to images table.

File: shared/schema.ts

TypeScript


export const images = pgTable("images", {
  id: serial("id").primaryKey(),
  userId: varchar("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  projectId: integer("project_id").references(() => projects.id, { onDelete: "cascade" }),
  parentImageId: integer("parent_image_id").references((): any => images.id, { onDelete: "cascade" }),
  isOriginal: integer("is_original").notNull().default(1),
  originalUrl: text("original_url").notNull(),
  currentUrl: text("current_url").notNull(),
  thumbnailUrl: text("thumbnail_url"),  // ← ADD THIS LINE
  fileName: varchar("file_name").notNull(),
  fileSize: integer("file_size").notNull(),
  width: integer("width").notNull(),
  height: integer("height").notNull(),
  createdAt: timestamp("created_at").defaultNow(),
  updatedAt: timestamp("updated_at").defaultNow(),
}, (table) => [
  // ... existing indexes
]);


Apply migration:

Bash


npm run db:push





Step 3: Create Thumbnail Generation Utility

File: server/thumbnailGenerator.ts (NEW FILE)

TypeScript


import sharp from 'sharp';
import { File } from "@google-cloud/storage";
import { objectStorageClient } from "./objectStorage";

export interface ThumbnailOptions {
  width?: number;
  height?: number;
  quality?: number;
  format?: 'webp' | 'jpeg' | 'png';
}

export class ThumbnailGenerator {
  private defaultOptions: Required<ThumbnailOptions> = {
    width: 400,
    height: 225,
    quality: 80,
    format: 'webp',  // ← WebP by default
  };

  /**
   * Generate WebP thumbnail from a Google Cloud Storage file
   * @param sourceFile - Source image file from GCS
   * @param destinationPath - Full GCS path for thumbnail (e.g., "/bucket/uploads/123/thumb.webp")
   * @param options - Thumbnail generation options
   * @returns Public URL of the generated thumbnail
   */
  async generateThumbnail(
    sourceFile: File,
    destinationPath: string,
    options?: ThumbnailOptions
  ): Promise<string> {
    const opts = { ...this.defaultOptions, ...options };

    try {
      console.log(`[ThumbnailGenerator] Generating ${opts.format} thumbnail for ${sourceFile.name}`);
      console.log(`[ThumbnailGenerator] Target size: ${opts.width}x${opts.height}`);
      console.log(`[ThumbnailGenerator] Destination: ${destinationPath}`);

      // Download original image to buffer
      const [imageBuffer] = await sourceFile.download();
      console.log(`[ThumbnailGenerator] Downloaded original (${(imageBuffer.length / 1024 / 1024).toFixed(2)} MB)`);

      // Get original image metadata
      const metadata = await sharp(imageBuffer).metadata();
      console.log(`[ThumbnailGenerator] Original dimensions: ${metadata.width}x${metadata.height}`);

      // Generate thumbnail using Sharp
      const thumbnailBuffer = await sharp(imageBuffer)
        .resize(opts.width, opts.height, {
          fit: 'cover',  // Crop to exact dimensions (maintains aspect ratio)
          position: 'center',  // Center crop
        })
        .toFormat(opts.format, {
          quality: opts.quality,
          // WebP-specific optimizations
          ...(opts.format === 'webp' && {
            effort: 4,  // Compression effort (0-6, higher = smaller file but slower)
            smartSubsample: true,  // Better quality at low bitrates
          }),
        })
        .toBuffer();

      const thumbnailSizeKB = (thumbnailBuffer.length / 1024).toFixed(1);
      const compressionRatio = ((1 - thumbnailBuffer.length / imageBuffer.length) * 100).toFixed(1);
      
      console.log(`[ThumbnailGenerator] Generated thumbnail (${thumbnailSizeKB} KB)`);
      console.log(`[ThumbnailGenerator] Compression: ${compressionRatio}%`);

      // Parse destination path
      const { bucketName, objectName } = this.parseObjectPath(destinationPath);
      const bucket = objectStorageClient.bucket(bucketName);
      const thumbnailFile = bucket.file(objectName);

      // Upload thumbnail to GCS
      await thumbnailFile.save(thumbnailBuffer, {
        metadata: {
          contentType: `image/${opts.format}`,
          cacheControl: 'public, max-age=31536000',  // Cache for 1 year
          metadata: {
            originalFile: sourceFile.name,
            generatedAt: new Date().toISOString(),
            width: opts.width.toString(),
            height: opts.height.toString(),
            format: opts.format,
            originalWidth: metadata.width?.toString() || 'unknown',
            originalHeight: metadata.height?.toString() || 'unknown',
          },
        },
        public: true,  // Make thumbnail publicly accessible
      });

      console.log(`[ThumbnailGenerator] ✓ Uploaded thumbnail to ${destinationPath}`);

      // Return public URL
      const publicUrl = `https://storage.googleapis.com/${bucketName}/${objectName}`;
      return publicUrl;

    } catch (error ) {
      console.error('[ThumbnailGenerator] ✗ Error generating thumbnail:', error);
      throw new Error(`Failed to generate thumbnail: ${error instanceof Error ? error.message : 'Unknown error'}`);
    }
  }

  /**
   * Generate WebP thumbnail from uploaded image during upload flow
   * @param originalFile - Original uploaded file
   * @param uploadId - Unique upload ID
   * @param privateObjectDir - Private object directory from env
   * @returns Thumbnail URL
   */
  async generateUploadThumbnail(
    originalFile: File,
    uploadId: string,
    privateObjectDir: string
  ): Promise<string> {
    // Create thumbnail path: /bucket/uploads/{uploadId}/thumb.webp
    const thumbnailPath = `${privateObjectDir}/uploads/${uploadId}/thumb.webp`;
    
    return this.generateThumbnail(originalFile, thumbnailPath, {
      width: 400,   // Matches gallery card width (16:9 aspect ratio)
      height: 225,  // Matches gallery card height
      quality: 80,  // Good balance between quality and size
      format: 'webp',  // Modern format, 30% smaller than JPEG
    });
  }

  /**
   * Generate WebP thumbnail from edit result
   * @param editResultFile - Edit result file
   * @param editId - Edit ID
   * @param privateObjectDir - Private object directory from env
   * @returns Thumbnail URL
   */
  async generateEditThumbnail(
    editResultFile: File,
    editId: string,
    privateObjectDir: string
  ): Promise<string> {
    const thumbnailPath = `${privateObjectDir}/edits/${editId}/thumb.webp`;
    
    return this.generateThumbnail(editResultFile, thumbnailPath, {
      width: 400,
      height: 225,
      quality: 80,
      format: 'webp',
    });
  }

  private parseObjectPath(path: string): { bucketName: string; objectName: string } {
    if (!path.startsWith("/")) {
      path = `/${path}`;
    }
    const pathParts = path.split("/");
    if (pathParts.length < 3) {
      throw new Error("Invalid path: must contain at least a bucket name");
    }

    const bucketName = pathParts[1];
    const objectName = pathParts.slice(2).join("/");

    return { bucketName, objectName };
  }
}

// Export singleton instance
export const thumbnailGenerator = new ThumbnailGenerator();





Step 4: Integrate Thumbnail Generation into Upload Flow

File: server/routes.ts

Find the image upload endpoint and add thumbnail generation:

TypeScript


import { thumbnailGenerator } from "./thumbnailGenerator";
import { ObjectStorageService } from "./objectStorage";

// ... existing code ...

// Example: Image upload endpoint
app.post("/api/images/upload", async (req, res) => {
  try {
    const { userId } = req.user;  // From auth middleware
    const { uploadUrl, fileName, fileSize, width, height } = req.body;

    // 1. Get the uploaded file from object storage
    const objectStorageService = new ObjectStorageService();
    const uploadedFile = await objectStorageService.getObjectEntityFile(uploadUrl);

    // 2. Generate WebP thumbnail ← NEW!
    const uploadId = extractUploadId(uploadUrl);  // Extract ID from URL
    const privateObjectDir = objectStorageService.getPrivateObjectDir();
    
    console.log('[Upload] Generating WebP thumbnail...');
    const startTime = Date.now();
    
    const thumbnailUrl = await thumbnailGenerator.generateUploadThumbnail(
      uploadedFile,
      uploadId,
      privateObjectDir
    );
    
    const thumbnailTime = Date.now() - startTime;
    console.log(`[Upload] ✓ Thumbnail generated in ${thumbnailTime}ms:`, thumbnailUrl);

    // 3. Save to database with thumbnail URL
    const image = await db.insert(images).values({
      userId,
      originalUrl: uploadUrl,
      currentUrl: uploadUrl,
      thumbnailUrl,  // ← NEW!
      fileName,
      fileSize,
      width,
      height,
      isOriginal: 1,
    }).returning();

    res.json({ success: true, image: image[0] });

  } catch (error) {
    console.error('[Upload] Error:', error);
    res.status(500).json({ error: 'Failed to upload image' });
  }
});

// Helper function to extract upload ID from URL
function extractUploadId(url: string): string {
  // Example URL: /objects/uploads/abc-123-def/original.jpg
  // Extract: abc-123-def
  const match = url.match(/\/uploads\/([^\/]+)/);
  if (!match) {
    throw new Error('Invalid upload URL format');
  }
  return match[1];
}





Step 5: Update Gallery to Use WebP Thumbnails

File: client/src/pages/GalleryPage.tsx or client/src/components/ProjectCard.tsx

Current code (ProjectCard.tsx, line 23):

TypeScript


<img
  src={thumbnailUrl}  // ← Currently uses full-size image
  alt={projectName}
  className="w-full h-full object-cover"
/>


Updated code:

TypeScript


<img
  src={project.originalImage?.thumbnailUrl || project.originalImage?.currentUrl || ""}  // ← Use thumbnail first, fallback to original
  alt={projectName}
  className="w-full h-full object-cover"
  loading="lazy"  // ← Add lazy loading
  data-testid={`img-project-${project.id}`}
/>


Fallback logic:

•
If thumbnailUrl exists → use WebP thumbnail (25 KB, fast!)

•
If thumbnailUrl is null (old images) → use currentUrl (slow, but works)

Browser compatibility:

•
WebP support: 96%+ (Chrome, Firefox, Safari, Edge)

•
For old browsers: fallback to original image automatically




Step 6: Add Thumbnail Generation to Edit Flow

File: server/routes.ts (or wherever AI edit results are saved)

TypeScript


// After AI generates edited image
app.post("/api/edits/save", async (req, res) => {
  try {
    const { userId, imageId, editResultUrl, prompt } = req.body;

    // 1. Get edit result file
    const objectStorageService = new ObjectStorageService();
    const editResultFile = await objectStorageService.getObjectEntityFile(editResultUrl);

    // 2. Generate WebP thumbnail for edit ← NEW!
    const editId = extractEditId(editResultUrl);
    const privateObjectDir = objectStorageService.getPrivateObjectDir();
    
    console.log('[Edit] Generating WebP thumbnail for edit result...');
    const thumbnailUrl = await thumbnailGenerator.generateEditThumbnail(
      editResultFile,
      editId,
      privateObjectDir
    );
    console.log('[Edit] ✓ Thumbnail generated:', thumbnailUrl);

    // 3. Save edit with thumbnail
    const edit = await db.insert(edits).values({
      userId,
      imageId,
      savedImageId: null,  // Will be updated later
      prompt,
      resultUrl: editResultUrl,
      thumbnailUrl,  // ← NEW!
      createdAt: new Date(),
    }).returning();

    res.json({ success: true, edit: edit[0] });

  } catch (error) {
    console.error('[Edit] Error:', error);
    res.status(500).json({ error: 'Failed to save edit' });
  }
});

function extractEditId(url: string): string {
  const match = url.match(/\/edits\/([^\/]+)/);
  if (!match) {
    throw new Error('Invalid edit URL format');
  }
  return match[1];
}





Step 7: Backfill Thumbnails for Existing Images (Optional)

Create a migration script to generate WebP thumbnails for existing images.

File: server/scripts/backfillThumbnails.ts (NEW FILE)

TypeScript


import { db } from "../db";
import { images } from "../../shared/schema";
import { thumbnailGenerator } from "../thumbnailGenerator";
import { ObjectStorageService } from "../objectStorage";
import { isNull, eq } from "drizzle-orm";

async function backfillThumbnails() {
  console.log('[Backfill] Starting WebP thumbnail generation for existing images...');

  // Find all images without thumbnails
  const imagesWithoutThumbnails = await db
    .select()
    .from(images)
    .where(isNull(images.thumbnailUrl))
    .limit(100);  // Process in batches to avoid memory issues

  console.log(`[Backfill] Found ${imagesWithoutThumbnails.length} images without thumbnails`);

  if (imagesWithoutThumbnails.length === 0) {
    console.log('[Backfill] No images to process. Exiting.');
    return;
  }

  const objectStorageService = new ObjectStorageService();
  const privateObjectDir = objectStorageService.getPrivateObjectDir();

  let successCount = 0;
  let errorCount = 0;

  for (const image of imagesWithoutThumbnails) {
    try {
      console.log(`[Backfill] Processing image ${image.id} (${successCount + errorCount + 1}/${imagesWithoutThumbnails.length})...`);

      // Get original file
      const originalFile = await objectStorageService.getObjectEntityFile(image.originalUrl);
      
      // Generate WebP thumbnail
      const uploadId = extractUploadId(image.originalUrl);
      const thumbnailUrl = await thumbnailGenerator.generateUploadThumbnail(
        originalFile,
        uploadId,
        privateObjectDir
      );

      // Update database
      await db
        .update(images)
        .set({ thumbnailUrl })
        .where(eq(images.id, image.id));

      console.log(`[Backfill] ✓ Generated thumbnail for image ${image.id}`);
      successCount++;

    } catch (error) {
      console.error(`[Backfill] ✗ Failed for image ${image.id}:`, error);
      errorCount++;
    }
  }

  console.log(`[Backfill] Complete! Success: ${successCount}, Errors: ${errorCount}`);
  
  if (imagesWithoutThumbnails.length === 100) {
    console.log('[Backfill] More images may need processing. Run script again.');
  }
}

// Run if called directly
if (require.main === module) {
  backfillThumbnails()
    .then(() => {
      console.log('[Backfill] Script finished successfully');
      process.exit(0);
    })
    .catch((error) => {
      console.error('[Backfill] Fatal error:', error);
      process.exit(1);
    });
}

function extractUploadId(url: string): string {
  const match = url.match(/\/uploads\/([^\/]+)/);
  if (!match) {
    throw new Error('Invalid upload URL format');
  }
  return match[1];
}

export { backfillThumbnails };


Run backfill:

Bash


npx tsx server/scripts/backfillThumbnails.ts


For large datasets (1000+ images):

Bash


# Run multiple times until no more images to process
while npx tsx server/scripts/backfillThumbnails.ts | grep -q "Found [1-9]"; do
  echo "Processing next batch..."
  sleep 2
done





COMPLETE WORKFLOW

Upload Flow (with WebP thumbnails):

Plain Text


1. User uploads image (7 MB JPEG)
     ↓
2. Server receives upload
     ↓
3. Save original to GCS
   /uploads/abc-123/original.jpg (7 MB)
     ↓
4. Generate WebP thumbnail ← NEW!
   - Download original
   - Resize to 400x225 (16:9 aspect ratio)
   - Convert to WebP format
   - Compress to 80% quality
   - Upload to GCS
   /uploads/abc-123/thumb.webp (25 KB) ✅
     ↓
5. Save to database:
   {
     originalUrl: "/objects/uploads/abc-123/original.jpg",
     thumbnailUrl: "/objects/uploads/abc-123/thumb.webp" ← NEW!
   }
     ↓
6. Gallery loads WebP thumbnail (25 KB) ✅ BLAZING FAST!
7. Editor loads original JPEG (7 MB) when needed


Edit Flow (with WebP thumbnails):

Plain Text


1. User edits image with AI
     ↓
2. AI generates result (5 MB)
     ↓
3. Save result to GCS
   /edits/def-456/result.jpg (5 MB)
     ↓
4. Generate WebP thumbnail ← NEW!
   /edits/def-456/thumb.webp (20 KB)
     ↓
5. Update database:
   {
     resultUrl: "/objects/edits/def-456/result.jpg",
     thumbnailUrl: "/objects/edits/def-456/thumb.webp" ← NEW!
   }
     ↓
6. Edit history shows WebP thumbnail (20 KB) ✅ FAST!





TESTING CHECKLIST

After implementing WebP thumbnails:

1. Upload new image:




Upload completes successfully




Check database: thumbnailUrl is populated with .webp extension




Check GCS: thumbnail file exists at /uploads/{id}/thumb.webp




Thumbnail is ~20-30 KB (not MB)




Thumbnail dimensions are 400x225px




Console shows: "Generated thumbnail (XX KB)" with compression ratio

2. Gallery display:




Gallery loads WebP thumbnails (not full images)




Check DevTools Network tab:

•
Images are ~25 KB each

•
Content-Type: image/webp

•
Status: 200 OK






Gallery loads in < 2 seconds (3G)




Images look sharp and clear (no pixelation)




Aspect ratio is correct (16:9, no stretching)

3. Editor display:




Editor loads full-size original image (not thumbnail)




Image quality is high in editor




Edit history shows WebP thumbnails




Thumbnails load instantly in edit history

4. Fallback for old images:




Old images without thumbnails still display




Uses currentUrl as fallback




No broken images or errors

5. Browser compatibility:




Test in Chrome (should show WebP)




Test in Firefox (should show WebP)




Test in Safari (should show WebP)




Test in Edge (should show WebP)

6. Performance:




Gallery: 9 images load in ~1.5 seconds (vs 40 seconds before)




Total download: ~225 KB (vs 63 MB before)




96% improvement ✅

7. Backfill script:




Run backfill script successfully




Old images now have thumbnails




No errors in console




Database updated correctly




EXPECTED RESULTS

Performance Comparison

Metric
Before
After (WebP)
Improvement
Image size
7 MB
25 KB
99.6% smaller
9 images total
63 MB
225 KB
99.6% smaller
Load time (3G)
40 sec
1.5 sec
96.3% faster
Load time (4G)
15 sec
0.5 sec
96.7% faster
Load time (WiFi)
5 sec
0.3 sec
94% faster


Format Comparison

Format
Size (400x225, 80% quality)
Browser Support
WebP
25 KB ✅
96%+
JPEG
50 KB
100%
PNG
120 KB
100%


WebP wins: 50% smaller than JPEG, 79% smaller than PNG!

Storage Impact

Additional storage per image:

•
Original: 7 MB

•
WebP thumbnail: 25 KB

•
Total: 7.025 MB (+0.36% overhead)

For 1000 images:

•
Originals: 7 GB

•
WebP thumbnails: 25 MB

•
Total: 7.025 GB (negligible overhead)

Processing Time

WebP thumbnail generation:

•
Sharp processing: ~150-250ms per image

•
Upload to GCS: ~200-300ms

•
Total: ~350-550ms added to upload time

User impact:

•
Upload time: +0.5 seconds (acceptable)

•
Gallery speed: -38.5 seconds (HUGE win!)




WHY WEBP?

Technical Advantages

1.
Superior Compression

•
Uses VP8 video codec compression

•
Predictive coding for better compression

•
25-35% smaller than JPEG at same quality



2.
Better Quality

•
Less blocking artifacts than JPEG

•
Smoother gradients

•
Better color preservation



3.
Modern Standard

•
Created by Google (2010)

•
Widely adopted (YouTube, Facebook, etc.)

•
Part of modern web stack



Browser Support (2025)

•
✅ Chrome: 100%

•
✅ Firefox: 100%

•
✅ Safari: 100% (since iOS 14)

•
✅ Edge: 100%

•
✅ Opera: 100%

•
⚠️ IE11: No (but IE is dead)

Total coverage: 96%+ of users

Fallback Strategy

If WebP fails to load (extremely rare):

TypeScript


thumbnailUrl || currentUrl  // Automatic fallback to original


No special <picture> tag needed - browser handles it automatically.




SUMMARY

What we're adding:

1.
✅ Sharp library for image processing

2.
✅ thumbnailUrl column in database

3.
✅ ThumbnailGenerator utility class with WebP support

4.
✅ Automatic WebP thumbnail generation on upload (400x225px)

5.
✅ Automatic WebP thumbnail generation for AI edits

6.
✅ Gallery uses WebP thumbnails instead of full images

7.
✅ Backfill script for existing images

Benefits:

•
✅ 96% faster gallery loading (40s → 1.5s on 3G)

•
✅ 99.6% less bandwidth usage (63 MB → 225 KB)

•
✅ 50% smaller than JPEG thumbnails (WebP vs JPEG)

•
✅ Better user experience on slow connections

•
✅ Minimal storage overhead (0.36%)

•
✅ Backward compatible (fallback to original)

•
✅ Future-proof (WebP is modern standard)

Technical specs:

•
Format: WebP

•
Size: 400x225px (16:9 aspect ratio)

•
Quality: 80%

•
Average file size: ~25 KB

•
Compression effort: 4 (balanced)

•
Cache: 1 year (public, immutable)

Time estimate: 3-4 hours

Risk: Low - additive change with automatic fallback




Ready to make the gallery blazing fast with WebP! 🚀

