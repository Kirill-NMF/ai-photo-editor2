ISSUE: The application is receiving a 410 Gone error from the Hugging Face API. The error message explicitly states that https://api-inference.huggingface.co is no longer supported and must be replaced with https://router.huggingface.co/hf-inference.

GOAL: Update the server/huggingface.ts file to use the new, correct Hugging Face API endpoint and adjust the request payload accordingly.

STRATEGY: We will modify the API_URL constant to point to the new router endpoint. The new endpoint is a generic router, so the model name, which was previously part of the URL, must now be moved into the JSON body of the POST request.

═══════════════════════════════════════════════════════════════════════ IMPLEMENTATION INSTRUCTIONS: ═══════════════════════════════════════════════════════════════════════

TASK 1: Overwrite the server/huggingface.ts File

1.
DELETE all existing code in server/huggingface.ts.

2.
COPY and PASTE the complete, corrected code from the snippet below into the file.

Why? This ensures the application uses the new, mandatory API endpoint and sends the request in the correct format that the new router expects.

═══════════════════════════════════════════════════════════════════════ CODE SNIPPET: server/huggingface.ts (Full Replacement Code ) ═══════════════════════════════════════════════════════════════════════

TypeScript


import fetch from 'node-fetch';

// The NEW and CORRECT router endpoint for all models.
const API_URL = 'https://router.huggingface.co/hf-inference';

interface HuggingFaceParams {
  imageUrl: string;
  prompt: string;
}

export async function editImageWithHuggingFace({ imageUrl, prompt }: HuggingFaceParams ): Promise<{ imageUrl: string }> {
  console.log('Attempting to edit image with Hugging Face using the new router endpoint...');

  try {
    const response = await fetch(API_URL, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.HUGGINGFACE_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        // The model is now specified in the body, not the URL.
        model: 'nvidia/ChronoEdit-14B-Diffusers',
        inputs: {
          prompt: prompt,
          image_url: imageUrl,
        },
      }),
    });

    if (!response.ok) {
      const errorBody = await response.text();
      console.error(`Hugging Face API responded with status ${response.status}:`, errorBody);
      throw new Error(`API request failed with status ${response.status}: ${errorBody}`);
    }

    const result = await response.json();

    if (result && result.image_url) {
      console.log('Successfully received edited image from Hugging Face.');
      return { imageUrl: result.image_url };
    } else if (result && result.error) {
      console.error('Hugging Face API returned an error:', result.error);
      throw new Error(`Hugging Face API error: ${result.error}`);
    } else {
      console.error('Unexpected response format from Hugging Face:', result);
      throw new Error('Unexpected response format from Hugging Face API.');
    }
  } catch (error: any) {
    console.error('An exception occurred in editImageWithHuggingFace:', error);
    throw new Error(`Failed to edit image with Hugging Face: ${error.message}`);
  }
}


